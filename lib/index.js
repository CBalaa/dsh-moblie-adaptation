/**
 * dsh-mobile-adaptation — host (Node) half.
 *
 * All behavior lives in the browser half (lib/client.js); the host half is a
 * valid empty Cordis plugin so the Loader can mount the entry this bundle's
 * cordis.patch.yml inserts.
 */
export function apply() {}
